{
    "venv": ".venv",
    "python": "python3.12",
    "env": {
        "PYTHONPATH": "./src"
    },
    "activate": true,
    "scripts": {
        "post-install": "python -m playwright install chromium && python -m patchright install chromium",
        "post-sync": "source .venv/bin/activate"
    }
}
